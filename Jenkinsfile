node('master'){
  stage('build'){
    sh 'echo build'
  }
  stage('docker image'){
    sh 'echo docker image'
  }
  stage('docker push'){
    sh 'echo push docker image'
  }
  stage('deploy'){
    sh 'echo deploy'
  }
}

